{
  "name": "TYGR Ventures",
  "install": "if [ ! -f .env ] && [ -f .env.example ]; then cp .env.example .env; fi\nnpm ci",
  "terminals": [
    {
      "name": "dev",
      "command": "npm run dev -- --host 0.0.0.0 --port 5173",
      "description": "Vite dev server for the TYGR Ventures site (React + mocked PHP API) on http://localhost:5173"
    }
  ],
  "ports": [
    { "name": "web", "port": 5173 }
  ],
  "egressMode": "parent_plus_network_settings",
  "egressAllowlist": [
    "tidycal.com",
    "www.tidycal.com"
  ]
}
